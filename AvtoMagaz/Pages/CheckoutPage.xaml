<Page x:Class="AvtoMagaz.Pages.CheckoutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AvtoMagaz.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CheckoutPage">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
        <TextBlock Text="Подтверждение заказа" FontSize="20" Margin="10"/>
        <ListView x:Name="lvOrderItems" Margin="5" MaxHeight="200">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Товар" DisplayMemberBinding="{Binding ProductName}" Width="150"/>
                    <GridViewColumn Header="Кол-во" DisplayMemberBinding="{Binding Quantity}" Width="60"/>
                    <GridViewColumn Header="Цена" DisplayMemberBinding="{Binding Price, StringFormat={}{0:C}}" Width="80"/>
                    <GridViewColumn Header="Сумма" DisplayMemberBinding="{Binding Total, StringFormat={}{0:C}}" Width="80"/>
                </GridView>
            </ListView.View>
        </ListView>
        <TextBlock x:Name="txtTotal" Text="Итого: 0 руб." FontWeight="Bold" Margin="5"/>
        <TextBlock Text="Подтвердите оформление заказа" Margin="5"/>
        <Button Content="Оформить заказ" Padding="10,5" Margin="5" Click="ConfirmOrder_Click"/>
        <Button Content="Вернуться в корзину" Padding="10,5" Margin="5" Click="BackToCart_Click"/>
    </StackPanel>
</Page>
